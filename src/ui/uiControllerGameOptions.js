export function applyUIControllerScenarioOptions(controller, scenarios, currentScenarioId) {
  controller.gameUI.setScenarioOptions(scenarios, currentScenarioId);
}

export function applyUIControllerBalanceProfileOptions(controller, profiles, currentProfileId) {
  controller.gameUI.setBalanceProfileOptions(profiles, currentProfileId);
}

